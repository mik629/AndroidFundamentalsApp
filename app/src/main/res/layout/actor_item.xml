<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/activity_medium_margin"
    android:backgroundTint="@color/dark_grey"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ava"
            android:layout_width="@dimen/ava_size"
            android:layout_height="@dimen/ava_size"
            android:background="@drawable/actor_bgd"
            android:scaleType="centerCrop"
            tools:src="@drawable/robert_downey_jr" />

        <TextView
            android:id="@+id/name"
            style="@style/ActorName"
            android:layout_width="match_parent"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:maxLines="2"
            tools:text="Mister Superman Junior" />
    </LinearLayout>

</androidx.cardview.widget.CardView>
